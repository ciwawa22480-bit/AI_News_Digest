name: AI News Digest

on:
  schedule:
    # GitHub 定时任务(cron)不保证准时，整点 00:00 UTC 是全球高峰最易被延迟/丢弃。
    # 改到错峰时间，并加一条备份触发，降低"某天没跑=当天没数据"的概率。
    - cron: '35 1 * * *'   # UTC 01:35 = 北京时间 09:35（主）
    - cron: '35 6 * * *'   # UTC 06:35 = 北京时间 14:35（备份，当天重跑覆盖同日文件）
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - 'config.json'
      - 'fetch_multi_source.py'
      - 'generate_ai_summary.py'
      - 'build_html.py'
      - '.github/workflows/daily-news.yml'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  generate-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch news
        run: python fetch_multi_source.py

      - name: AI curate and summarize
        run: python generate_ai_summary.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}

      - name: Build page
        run: python build_html.py

      - name: Include data directory in published output
        run: |
          mkdir -p output/data
          cp -r data/. output/data/

      - name: Commit data
        run: |
          git config user.name "AI News Bot"
          git config user.email "bot@users.noreply.github.com"
          git add data/ || true
          git diff --cached --quiet || git commit -m "update data $(date +%Y-%m-%d)"
          git push || true

      - name: Deploy to Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output
          force_orphan: true
